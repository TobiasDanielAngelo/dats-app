import { AccountComponents } from "./AccountComponents";
import { CategoryComponents } from "./CategoryComponents";
import { PayableComponents } from "./PayableComponents";
import { ReceivableComponents } from "./ReceivableComponents";
import { TransactionComponents } from "./TransactionComponents";

export const Finance = {
  Account: AccountComponents,
  Category: CategoryComponents,
  Payable: PayableComponents,
  Receivable: ReceivableComponents,
  Transaction: TransactionComponents,
};
